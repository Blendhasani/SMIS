@model WebApplication5.Models.Transkripta

@{
    ViewData["Title"] = "Create";
    Student s = new Student();
    Transkripta t = new Transkripta();
}

<h4 class="mt-3">Paraqit provimet</h4>
<hr />
<div class="row" >
    <div class="col-md-12">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (User.IsInRole("Teacher"))
            {
                <div class="form-group">
                    <label asp-for="Nota" class="control-label"></label>
                    <input asp-for="Nota" class="form-control" />
                    <span asp-validation-for="Nota" class="text-danger"></span>
                </div>
            }
         
         @*   <div class="form-group">
                <label asp-for="Student.Name" class="control-label"></label>
                <select asp-for="StudentId" class ="form-control" asp-route-id="@Student.Id"></select>
            </div>*@
       
           
            <div class="form-group" style="width:80%;display:flex;flex-direction:row;justify-content:space-evenly;  margin:auto;padding:10px;" >

 <label asp-for="Subject.Name" style="margin-right:15px;margin-top:5px;" class="control-label"></label>
                <select asp-for="SubjectId" class ="form-control form-select" style="margin-right:15px;" asp-items="ViewBag.SubjectId"></select>
           
                <input type="submit" value="Paraqit" class="btn btn-primary" />
            </div>
          
         
        </form>
    </div>
</div>
@*
<div>
    <a asp-action="Index">Kthehu prapa</a>
</div>
*@
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
